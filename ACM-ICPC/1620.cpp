#include <iostream>
#include <string>
#include <vector>
using namespace std;

#pragma warning(disable:4996)

int n, m;
vector<string> v;

int main() {
	cin >> n >> m;

	for (int i = 0; i < n; i++) {
		string s;
		cin >> s;
		v.push_back(s);
	}

	for (int i = 0; i < m; i++) {
		
	}
}