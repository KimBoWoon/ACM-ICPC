#include <cstdio>
#include <stack>

using namespace std;

#pragma warning(disable:4996)

int main() {
	int n, temp;
	int *arr;
	stack<int> s;

	scanf("%d", &n);

	while (!s.empty()) {
		
	}

	return 0;
}